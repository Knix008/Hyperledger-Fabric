This directory will have real project example based on Docker Swarm mode. 
You need set to NFS for this example and run "docker swarm init" first. Then join other nodes into the Docker swarm.

Thanks.
